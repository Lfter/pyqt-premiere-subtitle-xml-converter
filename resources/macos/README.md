# macOS Icon Resources

`AppIcon.iconset` contains the PNG files required by macOS `iconutil`.

On macOS, compile the final app icon with:

```bash
iconutil -c icns AppIcon.iconset
```

`app_icon_master_1024.png` is the editable high-resolution source image.
